spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL}

  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  jwt:
    secret: ${JWT_SECRET}

  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}

discord:
  webhook: https://discord.com/api/webhooks/1190291470452793475/CnGU1kIFs7cXCPnJQRjoKD2RLR_3JNWp7C_Ag00Dc2pUsMAnxjDjIQVStT8m1OLfLUPj
